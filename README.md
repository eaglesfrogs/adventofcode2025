# adventofcode2025

Create `.env` file based on the example

Login to adventofcode.com and get the session token from the browser's dev console and populate in the `.env` file.

Execute... `go run main.go -day 1`

Input data will be downloaded to a local directory `./.input` and it will run each the puzzle for the specified day.

